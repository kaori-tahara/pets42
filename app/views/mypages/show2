   <div class ="history-past">
      <div class = "craving_history_past">
        <div>過去のお散歩実績</div>
       <ul class ="history_list">
               <%if @craving_histories == nil  %>
                <div>過去のお散歩実績はありません</div>
                <%else%>
                 <li><% @craving_histories.each do |craving_history| %> 
                 <div><%=craving_history.created_at.strftime('%Y/%m/%d')%>済み</div>
                 </li>
                <%end%>
             <%end%>
       </ul>
      </div>
      <div class = "job_history_past">
        <div>過去の依頼実績</div>
       <ul class ="history_list">
          <%if @job_histories == nil %> 
          <div>過去の依頼実績はありません</div> 
           <%else%>
            <li> <% @job_histories.each do |job_history| %> 
            <div><%=job_history.created_at.strftime('%Y/%m/%d')%>済み</div>
             </li>
            <%end%>
          <%end%>
       </ul>
      </div>
    </div>